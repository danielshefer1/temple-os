#pragma once

// Keyboard input is delivered to drivers/tty.c via tty_input_byte.
// This header is intentionally empty; legacy declarations were removed
// along with the unsynchronized global console_buffer.
